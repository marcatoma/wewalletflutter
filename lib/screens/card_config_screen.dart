import 'package:flutter/material.dart';

class CardConfig extends StatelessWidget {
  const CardConfig({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Personalizar su tarjeta'),
      ),
    );
  }
}
